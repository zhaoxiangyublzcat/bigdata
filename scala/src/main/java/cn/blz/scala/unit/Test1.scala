package cn.blz.scala.unit

/**
 * @description
 * @className Test1
 * @author Blzcat
 * @date 2020.01.09 13:06
 */
object Test1 {
  def main(args: Array[String]): Unit = {
    // 1. 基础
    // 1.1 声明值和变量val、var，声明变量时都必须初始化。
    // 1.2 常用类型 Byte、Char、Short、Int、Long、Float、Double、Boolean
    // 1.3 算术和操作符重载
    //    println(1 + 1)
    //    println(1.+(1))
    // 1.4 apply、update
    //    println("test".updated(3,"a").apply(3))
    //    println(Array.apply(1, 2).update(3, 3))
    // 1.5 option类型
    // 2.  控制结构和函数
    // 2.1 If else 表达式，如果 if 或者 else 返回的类型不一样 ， 就 返回 Any 类型
    // 2.2 while 表达式
    // 2.3 for 表达式，生成器表达式 i <- 1 to 10，使用 yield 关键字便能在 for 表达式中生成新的集合
    // 2.4 函数，def 函数名(参数名：类型…): 返回类型 = 函数体
    // 2.5 过程
    // 2.6 懒值，用于初始化开销比较大的语句，解决循环依赖问题
    // 3. 数据结构
    // 3.1 主要的集合特质
    // 3.2 数组
    // 3.2.1 定长数组，Array
    // 3.2.2 变长数组，ArrayBuffer
    // 3.2.3 多维数组，ofDim 方法
    // 3.3 映射 Map
    // 3.4 元组
    // 3.5 队列
    // 3.6 堆栈
    // 3.7 列表
    // 3.8 集
    // 4. 模式匹配
    // 4.1 switch



  }
}
